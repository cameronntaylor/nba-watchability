name: NBA Watchability Bot

on:
  workflow_dispatch:  # manual trigger (keep this!)
  
  schedule:
    # Mon–Fri at 12:00 PM PT
    # GitHub schedules are UTC and do not support PT DST directly, so we run at both possible UTC hours.
    - cron: "0 19 * * 1-5"  # 12:00 PDT
    - cron: "0 20 * * 1-5"  # 12:00 PST

    # Sat–Sun at 10:00 AM PT
    - cron: "0 17 * * 6,0"  # 10:00 PDT
    - cron: "0 18 * * 6,0"  # 10:00 PST

jobs:
  run-bot:
    runs-on: ubuntu-latest

    env:
      # SAFETY FLAGS
      USE_PLAYWRIGHT: "true"
      DRY_RUN: "false"

      # API keys (already stored as repo secrets)
      ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}

      TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
      TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
      TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
      TWITTER_ACCESS_SECRET: ${{ secrets.TWITTER_ACCESS_SECRET }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install playwright tweepy python-dotenv

      - name: Install Playwright browser
        run: python -m playwright install chromium

      - name: Run bot and post tweet
        run: python scripts/run_bot.py

      - name: Upload screenshot artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dashboard-screenshot
          path: |
            output/chart.png
            output/table.png
